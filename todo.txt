- notification could open the browser directly
- UI design needs to be perfected
- if we are looking at a number, it could go up and down,
 maybe we are only interested if it has gone up
    - site specific configuration, to send different 
    notifications for captcha, ignore if its not an actual change
    - original config options would be retained,
     or at least available through json configuration